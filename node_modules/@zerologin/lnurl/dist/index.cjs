var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/index.ts
var src_exports = {};
__export(src_exports, {
  decode: () => decode,
  encode: () => encode
});
module.exports = __toCommonJS(src_exports);
var import_bech32 = require("bech32");

// src/utils.ts
var getRootDomain = (url) => {
  if (!url.includes("://")) {
    url = "http://" + url;
  }
  const regex = /^.*:\/\/(?:[wW]{3}\.)?([^:\/]*).*$/gm;
  const result = regex.exec(url);
  if (result && result.length > 1) {
    return result[1];
  }
  throw new Error(`Cannot get root domain for "${url}"`);
};
var getLnurlObject = (params, obj) => {
  var _a;
  return (_a = params.get(obj)) != null ? _a : "";
};

// src/index.ts
var limit = 1023;
var prefix = "lnurl";
var decode = (lnurl) => {
  const protocol = "lightning:";
  if (lnurl.toLowerCase().includes(protocol)) {
    lnurl = lnurl.toLowerCase().split("lightning:")[1];
  }
  const decoded = import_bech32.bech32.decode(lnurl, limit);
  const decodedString = Buffer.from(import_bech32.bech32.fromWords(decoded.words)).toString("utf8");
  const split = decodedString.split("?");
  if (split.length !== 2) {
    throw new Error(`Cannot decode LNURL string ('${lnurl}')`);
  }
  const url = split[0];
  const domain = getRootDomain(url);
  const lnurlObject = new URLSearchParams(split[1]);
  return { decoded: decodedString, domain, tag: getLnurlObject(lnurlObject, "tag"), k1: getLnurlObject(lnurlObject, "k1"), action: getLnurlObject(lnurlObject, "action") };
};
var encode = (unencoded) => {
  let words = import_bech32.bech32.toWords(Buffer.from(unencoded, "utf8"));
  return import_bech32.bech32.encode(prefix, words, limit).toUpperCase();
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  decode,
  encode
});
//# sourceMappingURL=index.cjs.map